package com.exercise2.model;

/**
 * 
 * @author Lucas Abrão
 *
 */
public interface ParseMetacriticSonyModel {
	
	/**
	 * Accesses the URL and parses the HTML page into a Document object from the
	 * framework JSoup (HTML parser)
	 */
	void accessPage();
}
